import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/admin/cms/universities/')({
    component: () => <div />, // Placeholder, will be replaced by lazy component
})
